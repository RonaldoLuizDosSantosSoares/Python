# lista 1 de exercicios da faculdade

print ('Entre com o valor do produto')
preco = float(input())

if preco >= 100:
    preco = preco + (preco * 0.20)
    print(f'O preço inflacionado é: {preco:,.2f}')
    
else:
    preco = preco + (preco * 0.10)
    print(f'O preço inflacionado é: {preco:,.2f}')
